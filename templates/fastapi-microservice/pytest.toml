[pytest]
minversion = "9.1"
addopts = [
    "-ra",
    "--strict-config",
    "--strict-markers",
    "--cov=app",
    "--cov-report=term-missing:skip-covered",
    "--cov-report=xml",
]
asyncio_mode = "auto"
pythonpath = ["src"]
testpaths = ["tests"]
