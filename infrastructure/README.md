# Infrastructure

Infrastructure architecture, environment conventions, and shared platform documentation.

Terraform-managed resources live in `deploy/terraform/`. Kubernetes application charts live in `deploy/helm/`.